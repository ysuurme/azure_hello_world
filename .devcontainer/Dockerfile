FROM mcr.microsoft.com/devcontainers/python:3.10

# The base image already has Python 3.10. 
# We use the generic devcontainer features in devcontainer.json to install 
# Terraform, the Azure CLI, and the Azure Functions Core Tools.
# This Dockerfile is intentionally kept minimal to leverage community-maintained features.

ENV PYTHONUNBUFFERED=1
