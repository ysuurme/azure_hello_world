services:
  backend:
    build: .
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      # Injected from kv-platformy-dev at launch by `task dev`; never stored on disk.
      # The :? guard fails the run if the secret was not exported (i.e. not launched via the task).
      - AZURE_CLIENT_SECRET=${AZURE_CLIENT_SECRET:?run via 'task dev' so the SP secret is fetched from Key Vault}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/healthz')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  frontend:
    build: .
    command: ["streamlit", "run", "src/ui/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
    ports:
      - "8501:8501"
    environment:
      - BACKEND_URL=http://backend:8000
    depends_on:
      backend:
        condition: service_healthy
